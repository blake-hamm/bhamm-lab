# Start k3s vms
- name: Install k3s and copy .kube/config
  hosts: k3s
  roles:
    - xanmanning.k3s
    - kubeconfig
  tags: [bootstrap]
- name: Copy .kube/config
  hosts: k3s-master-0
  roles:
    - kubeconfig
  tags: [bootstrap, kubeconfig]

# Start argocd apps
