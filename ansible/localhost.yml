- name: Template preseed files
  hosts: localhost
  vars:
    # This will create preseed files for each of these hosts in /build/<hostname>-preseed.cfg
    preseed_hostname_disk_id_map:
      "aorus": "/dev/disk/by-id/ata-KINGSTON_SA400S37240G_50026B77845B74E3"
  roles:
    - preseed
  tags: [preseed]
