# tasks file for opnsense
- name: Configure vlans
  ansible.builtin.include_tasks:
    file: vlans.yml
    apply:
      tags: [vlan]
      notify: Reload vlan
- name: Configure aliases
  ansible.builtin.include_tasks:
    file: aliases.yml
    apply:
      tags: [aliases]
