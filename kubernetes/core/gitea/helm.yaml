apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gitea
  namespace: default
  annotations:
    argocd.argoproj.io/sync-wave: "200"
spec:
  destination:
    namespace: default
    server: https://kubernetes.default.svc
  project: default
  source:
    chart: gitea
    repoURL: https://dl.gitea.com/charts/
    targetRevision: v10.4.0
    helm:
      valuesObject:
        gitea:
          additionalConfigSources:
            - configMap:
                name: gitea-app-ini-plaintext
          oauth:
            - name: 'authelia'
              provider: 'openidConnect'
              existingSecret: 'gitea-external'
              autoDiscoverUrl: https://auth.bhamm-lab.com/.well-known/openid-configuration
              useCustomUrls: true
              customAuthUrl: 'https://auth.bhamm-lab.com/api/oidc/authorize'
              customTokenUrl: 'https://auth.bhamm-lab.com/api/oidc/token'
              customProfileUrl: 'https://auth.bhamm-lab.com/api/oidc/userinfo'
              customEmailUrl: 'https://auth.bhamm-lab.com/api/oidc/userinfo'
  syncPolicy:
    syncOptions:
      - ApplyOutOfSyncOnly=true
    automated:
      prune: true
      selfHeal: true
