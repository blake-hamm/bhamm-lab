apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-library-pvc
  annotations:
    argocd.argoproj.io/sync-wave: "250"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi
