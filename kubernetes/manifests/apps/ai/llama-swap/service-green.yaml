apiVersion: v1
kind: Service
metadata:
  name: llama-swap
  namespace: llama-swap
  annotations:
    argocd.argoproj.io/sync-wave: "23"
spec:
  type: ClusterIP
  selector:
    app: llama-swap
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
