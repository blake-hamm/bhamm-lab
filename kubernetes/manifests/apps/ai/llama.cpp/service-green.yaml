apiVersion: v1
kind: Service
metadata:
  name: llama-cpp-server
  namespace: llama-cpp
  annotations:
    argocd.argoproj.io/sync-wave: "23"
spec:
  type: ClusterIP
  selector:
    app: llama-cpp
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
