apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: llm-models-common
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "17"
spec:
  destination:
    namespace: models
    server: https://kubernetes.default.svc
  project: default
  source:
    repoURL: https://github.com/blake-hamm/bhamm-lab.git
    targetRevision: feature/pve-upgrade-9
    path: kubernetes/charts/common
    helm:
      valuesObject:
        name: models
        externalSecrets:
          enabled: true
          secrets:
            - secretKey: hf_token
              remoteRef:
                key: /default/llm-models
                property: hf_token
            - secretKey: s3_access_key
              remoteRef:
                key: /core/ceph
                property: s3_access_key
            - secretKey: s3_secret_key
              remoteRef:
                key: /core/ceph
                property: s3_secret_key
        postgresql:
          enabled: true
          storageSize: 20Gi
          backup:
            enabled: true
          restore:
            enabled: true
          additionalDatabases:
            - name: mlflow
            - name: phoenix
        ingressRoutes:
          - name: mlflow
            ingressClass: traefik-external
            websocket: true
            routes:
              - match: Host(`mlflow.bhamm-lab.com`)
                kind: Rule
                middlewares:
                  - name: default-headers
                services:
                  - name: llm-models-mlflow
                    scheme: http
                    port: http
          - name: phoenix
            ingressClass: traefik-external
            websocket: true
            routes:
              - match: Host(`phoenix.bhamm-lab.com`)
                kind: Rule
                middlewares:
                  - name: default-headers
                  - name: forwardauth-authelia
                services:
                  - name: llm-models-phoenix-svc
                    scheme: http
                    port: llm-models-phoenix-app
  syncPolicy:
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
