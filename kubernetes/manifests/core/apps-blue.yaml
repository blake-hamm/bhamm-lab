apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: blue-apps
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "30"
spec:
  project: default
  source:
    repoURL: https://github.com/blake-hamm/bhamm-lab.git
    targetRevision: feature/dr
    path: kubernetes/manifests/apps
    directory:
      recurse: true
      include: "{**all.yaml,**blue.yaml}"
  destination:
    server: https://kubernetes.default.svc
    namespace: argo
  syncPolicy:
    syncOptions:
      - ApplyOutOfSyncOnly=true
    automated:
      prune: true
      selfHeal: true
